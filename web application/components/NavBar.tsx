const NavBar: React.FC = () => {
  return (
    <nav className="container p-3 flex flex-row justify-between">
      <a className="uppercase font-bold">chopejobs</a>
      <ul className="flex flex-row"></ul>
    </nav>
  );
};

export default NavBar;
